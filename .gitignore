# dependencies
node_modules/

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# logs / scratch
*.log
/tmp/

# production build output (regenerated by `npm run build`)
dist/

# exe.dev API token (secret)
deploy/.api-token

# downloaded reference geometry (Sketchfab scans/models)
scan/*.glb
scan/*.gltf
scan/*.bin
scan/*.zip

# committed reference model for bee-test (CC-BY, credited)
!scan/honeybee_art.glb
!scan/honeybee_art.CREDIT.txt

# heavy reference corpus (16-insect pack: converted glbs + preview renders) — local only
scan/pack/
# python bytecode from headless scripts
__pycache__/
